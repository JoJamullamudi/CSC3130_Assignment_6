public class EncryptedBT {


    public boolean findElement( int[] tree, int element)
    {
        if( tree == null)
        {
            return false;
        }
        if ( tree.length == 0)
        {
            return false;
        }
        if ( tree[0] == -1)
        {
            return false;
        }

        return checkVal( )
    }

    public boolean checkVal


}
